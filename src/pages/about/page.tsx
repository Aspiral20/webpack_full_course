import React, { FC } from 'react';

interface PageProps {
}

const Page: FC<PageProps> = ({}) => {
  return (
    <div className="about_page">
      About page
    </div>
  );
};

export default Page;